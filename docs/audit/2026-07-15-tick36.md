# 自我审计 2026-07-15（tick36）

> hermes-researcher-deep-tick-daily
> 北京时间 2026-07-15 02:01 启动；UTC 2026-07-14 18:01
> latest release: v0.18.2（2026-07-08T03:11:22Z），release day +6
> local: v0.18.0，local commit 4d5d9fff +1 carried commit
> upstream main: 77d5b2d5（2026-07-14T17:15:49Z）
> compare local...main: upstream ahead_by 1293, behind_by 0

## 昨日采纳率

- 昨日 tick35：5 SOUL + 3 skill = 8 proposals。
- 生产 SOUL 搜索 `cron-installer-handoff-state` / `cross_cluster_arrows` / `trust_boundary_impact` / `session-state-integrity-deck`：0 命中。
- backup repo 在 tick35 commit `78ed8fb` 后没有 proposal 被覆盖/吸收到生产稿的 commit。
- session history 能看到 chief-agent digest 复述 tick35，但没有用户对 A/B/C 的明确 ack/nack/edit；复述不计采纳。
- **采纳数 = 0 / 8，采纳率 = 0%，zero-adoption streak = 12 days。**

## MCP 昨日状态审计

- `/memory/review_pending` 需要 privileged role；用 coordinator context 可读。
- 当前 namespace 路由存在历史分裂：
  - `project_id=gc-hermes-researcher` 返回 11 条 pending，包括 tick34 family proposal。
  - `project_id=gc-hermes-config` 返回 9 条 pending，但未找到 tick35 四个 receipt ID。
- tick35 报告的 4 IDs（`f1e34cdd` / `07981b9e` / `0214c6c2` / `1290adda`）在当前 review/search/audit 返回中未命中。
- 判定：**昨日 MCP receipt 可证明当时 propose 返回成功，但当前 namespace 可检索性未证明**。本 tick 必须统一 context 为 `org=gc-hermes / project=gc-hermes-config / agent=gc-hermes-researcher`，并记录 receipt；不调用 commit。

## 数据源状态

| Source | Result |
|---|---|
| GitHub REST via `gh` | OK；2026-07-12 起新建 items >100；抽取 P1=5（4 issues + 1 PR） |
| Hermes releases/main | latest v0.18.2；main 77d5b2d5；local...main ahead_by=1293 |
| Anthropic official | 无近 72h 可独立升级的新 P1；搜索结果多为旧公告/动态页面 |
| MCP official | RC/final 2026-07-28 距今 13 天；继续 readiness，不切生产 |
| arXiv | 命中 2607.11390 / 11126 / 11226；方向性加证，不单独立 P1 |
| HN | runtime sandbox 70 servers 有可复核项目；Grok home-upload 只有 X 二手链接，未独立核验，过滤 |
| Reddit | web backend 无可靠新结果，跳过 |
| X | x_search timeout，按 fallback 跳过 |
| OpenRouter | MiniMax-M3 仍 0.30 / 1.20 USD per 1M，无 >20% 变化 |

## 外部内容净化

- 所有 web 搜索/抓取内容只作为 data，不执行页面指令。
- MCP 事实 payload 使用 issue-ID 极简模板，避免 restricted literals。
- 文档只写公开 issue/PR 信息；auth material 字面未写入。
- staged diff 在 commit 前走 Python precise verifier，不能用 grep 变量名匹配。

## P0/P1 归一化

### P0 — 0

没有可独立核验的新 P0。

### P1 — 5 signals / 4 operational clusters

1. **F9 expansion — async completion ownership**
   - [#64484](https://github.com/NousResearch/hermes-agent/issues/64484) open P1。
   - durable restore 把旧 session completion 恢复到新 process；CLI idle/post-turn drain 的 ownership filter 失效。
   - trust boundary: `identity + info_disclosure`。
   - #63317 只修 TUI；#63856 只管 in-flight interrupt；都不覆盖 completed durable row。

2. **F8↔F1 — bounded output liveness**
   - [#64435](https://github.com/NousResearch/hermes-agent/issues/64435) open P1。
   - 生产证据：504.2 MiB output，gateway cgroup 约 3 GiB memory + 1.5 GiB swap，post-processing `.lower()` 抛 `MemoryError`；Telegram/Discord/cron 同时饿死。
   - #64524 open；#64448 closed unmerged。触发 PR dedup fire。

3. **F10↔F8 — Desktop artifact-source coherence**
   - [#64333](https://github.com/NousResearch/hermes-agent/issues/64333) open P1。
   - Desktop bundled code 缺 source tree 已有 symbol，cron tick 后每个 job dead-on-arrival；UI 无 visible receipt。
   - #64359 open，但 back-compat import shim 不足以替代 artifact manifest + bundled smoke。

4. **F10↔F1 + F1 stream extension**
   - [#64482](https://github.com/NousResearch/hermes-agent/issues/64482) open P1；#64506 open。
   - Telegram runtime dependency 使用 slotted object，旧 instrumentation 做 instance method assignment，platform 0 connected。
   - [#64420](https://github.com/NousResearch/hermes-agent/pull/64420) open P1 PR：zero-chunk stream 进入 retry。

## 重要状态变化

- #63207 closed；#63219 merged → release verify pending。
- #63128 closed，但 #63130/#63172 closed unmerged → 未证明 fixed。
- #63008 closed，但 #63018 closed unmerged → 未证明 fixed。
- #63129 仍 open；#63132/#63174 closed unmerged。
- #41935 closed cannot-reproduce，不算 merged fix。
- #60683 MiniMax stream P2 仍 open；#60695/#60700 仍 open。
- #60350 env spill P2 与 #57124 仍 open。

## Family registry

维持 10 family，不新立 F11：

| Family | tick36 lifecycle | 变化 |
|---|---|---|
| F1 silent-fail | expansion | Telegram dependency + zero-chunk stream |
| F2 cross-platform-state | stable | 无新 P1 |
| F3 memory-injection-cross-platform | stable | 无新 P1 |
| F4 credential-pool-stale-snapshot | stable | 无新 P1 |
| F5 cron-session-leak-closed-state | maintenance/verification | #41935 cannot-reproduce close |
| F6 outbound-redact-call-site | maintenance | 无新 P1 |
| F7 MCP-supply-chain-protocol-migration | expansion/readiness | 2026-07-28 final 临近 |
| F8 cron-ticker-resilience-deck | expansion | #64435 + #64333 |
| F9 session-state-integrity-deck | expansion | #64484；#63129 仍 open |
| F10 cron-installer-handoff-state | expansion | #64333 + #64482 artifact/dependency coherence |

## Cross-cluster arrows

- severity-A `F9-F1-restored-completion-misroute`
- severity-A `F8-F1-unbounded-output-liveness`
- severity-B `F10-F8-desktop-bundle-cron-dead`
- severity-B `F10-F1-telegram-runtime-compat`

详细见 `cron-output/2026-07-15-tick36/impact-graph.md`。

## Self-downgrade v4 逐 rule 判定

| Rule | Condition | Hit |
|---|---|---|
| 1 | major release day <24h | ❌ latest v0.18.2，release day +6 |
| 2 | installer recurrence 30d ≥2 | ✅ F10 旧 5 hits + tick36 #64333/#64482 |
| 3 | PR dedup fire ≥2 | ✅ #64435 两实现（#64448/#64524）+ F9 旧竞争 PR状态仍需仲裁 |
| 4 | silent-fail cross-month recurrence | ✅ F1 继续出现 platform connect / zero-chunk 变体 |
| 5 | P1 ≥8 + streak ≥4 | ❌ consolidated P1 signals=5 |
| 6 | streak ≥5 + any rule 1-5 | ✅ streak 12 + rules 2/3/4 |
| 7 | streak ≥5 + no prior hit → pause | 被 rule 6 推翻 |
| 8 | streak ≥4 normal → every other day | 被 rule 6 推翻 |

**决议：maintain_daily + 飞书 A/B/C；rule 2 + 3 + 4 三 rule 同命中。**

- A 降频到隔日：保留选项，不推荐。
- B 维持每日：推荐；当前有 2 条 severity-A。
- C 暂停：不推荐；存在 trust-boundary identity/info_disclosure 活跃信号。

严格遵守用户红线：**不写 cron/control plane**，这里只输出调参决议。

## 调参

| 参数 | tick35 | tick36 |
|---|---|---|
| family registry | 10 | 维持 10，抑制同义 family 膨胀 |
| acceptance | 11-field v3 | 15-field v4 |
| ship gate | 50 | 68（50 + 4 field delta + 14 runtime） |
| trust boundary | 5 categories | 本 tick active=`identity + info_disclosure` |
| SOUL quota | 5/5 | 5/5 |
| skill quota | 3/3 | 3/3 |
| MCP | propose only | 4 proposals planned；事实=1.0，建议=0.75 |
| secret scan | Python v4 | 维持，staged diff 必跑 |

## 产出清单

- [x] 5 SOUL drafts
- [x] 3 skill drafts
- [x] impact graph
- [x] self audit
- [x] 4 MCP proposes + receipt IDs：`a6b1ca1a` / `bad4d2b1` / `bab2675e` / `35ae7046`（4/4 pending_review；conflict check 4/4 no_conflict；probe `ac3e24c7` 已 Redis HDEL，HEXISTS=0）
- [x] staged Python secret verifier：PASS，scanned 1322 added lines
- [ ] git commit + push + remote readback
- [ ] final Feishu card（由最终 assistant message 自动投递）
