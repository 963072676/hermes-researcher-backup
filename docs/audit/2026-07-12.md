# 自我审计 2026-07-12 (tick33)

> hermes-researcher-deep-tick-daily tick33
> v0.18.2 (v2026.7.7.2) ship day +4 (last push 2026-07-11 17:17Z — upstream 8121dbb1)
> streak zero-adoption = **9 days** (tick25-33 全 0 → tick33 进入第 9 天)

## 指标对比

| 指标 | 昨日 (tick32) | tick33 | 趋势 | 状态 |
|---|---|---|---|---|
| 提议数 | 8 (5 SOUL + 3 skill) | 8 (5 SOUL + 3 skill) | = | OK |
| 采纳数 | TBD (pending_review 累计) | TBD | - | 仍待填 |
| 采纳率 | TBD | TBD | - | 仍待填 |
| 误报数 | 0 | 0 | = | OK |
| GH push 失败 | 0 | TBD | - | 待 tick 末确认 |
| P0 涌现 | 0 | 0 | = | OK |
| P1 涌现 | 4 (consolidated) | **4** (consolidated) | = | 持续 |
| P2 涌现 | ~38 | ~40 (估) | ↑ | 持续 |
| MCP propose_write | 4 active | **4 active** (77fe080a/5f36d52b/af81023e/b868be0c) | = | OK |
| PR-dedup fire (24h ≥ 3 PR) | 3 | **2** (env spill 4 PRs + gateway deadlock 1 PR) | ↓ | 持续 |
| installer-recurrence 30d | 2 | 2 | = | 持续 |
| cron-session-leak 30d (NEW) | 3 | 3 | = | 持续 |
| outbound-redact-call-site 30d | 1 | 1 | = | 持续 |
| NEW family 立卡 | cron-session-leak + outbound-redact + MCP 5-control (3) | **cron-ticker-resilience-deck (1)** | = | OK |
| v0.18.x ship | v0.18.2 day +2 | v0.18.2 day +4 | patch | 持续 |
| streak zero-adoption | 8 | **9** | ↑ +1 | **触发 v4 rule** |
| 8 family registry | 7 | **8** | ↑ +1 | NEW 立卡 |

## tick33 信号密度

- **GitHub**: 9 GH issues 1 family (`#32612 #32895 #37179 #27485 #11614 #48234 #49410 #30719 #44049`) — cron-ticker-resilience-deck NEW family 立卡
- **v0.18.2 ship day +4** — post-release rediscovery phase 持续
- **last push main**: 2026-07-11 17:17Z (upstream 8121dbb1) — 持续 high velocity
- **MCP supply chain**: GH #45620 (config v27→v28 mcp_servers injection) closed 但未根治;CVE-2025-54135 CurXecute + CVE-2025-54136 MCPoison + Snyk ToxicSkills + MCPTox + TIP + OX Security RCE 150M+ converge
- **silent-fail cluster 持续**: #62151 gateway deadlock + #62198 fix open + #60350 env spill + 4 PR dedup
- **OPENROUTER CN share** baseline metric 未跑(沿用 tick28/29/30 — 跳过 X search timeout)

## tick33 P0/P1 判定

### P0 (open) — 0

延续 tick26+ 的 "P0/P1 zero P0" 状态。

### P1 (open) — 4 (consolidated)

1. **cron-ticker-resilience-deck (NEW family)** — 9 GH issues 1 family
   - root cause: cron thread death + lock starvation + ownership race + restart loop + ticker visibility 4-轴失效
   - fix PRs: 多个 (#50016 BaseException catch / #44049 desktop defer / #27492 lock granularity / #32666 logging visibility) — 多数 open
   - **判定**: chief 6h dedup SLA — 选 primary `#50016` (canonical BaseException catch + ticker heartbeat) ,关重复
   - **NEW 立卡 family**: `cron-ticker-resilience-deck`
   - **sweeper marker**: `sweeper:risk-cron-ticker-resilience`

2. **MCP supply chain 6-control (升级)** — 6 source converge
   - root cause: 5 control (allowlist + hash pin + container + gateway + data-vs-commands) 缺第 6 (tirith pipe scan)
   - **判定**: tick32 立卡 5-control → 升级到 6-control,加 tirith pipe scan
   - **NEW sweeper marker**: `sweeper:risk-mcp-supply-chain-6-control`(沿用 tick32 5-control)

3. **silent-fail cron gateway still open** (沿用 tick27 family 升级)
   - root cause: v0.18.2 patch wave 未根治 silent-fail (env spill + gateway deadlock + MCP keepalive)
   - fix PRs: 多 (#60386 env spill / #62198 deadlock / #62220 MCP keepalive) — 多数 open
   - **判定**: dev 4 验收清单 (失败注入 + 日志可观测性 + delivery receipt + silent-drop counter)

4. **release verification v3** (沿用 tick27 ship gate 升级)
   - root cause: 5 grep checklist 缺第 6 (MCP supply chain 6-control scan) + 4 silent-fail scenario
   - **判定**: qa ship gate 升级 — 6 grep + 4 silent-fail scenario = 10 项全过才允许 ship

## NEW 立卡 family (tick33)

### cron-ticker-resilience-deck (8th family)

- **触发**: 9 GH issues 1 family (#32612 #32895 #37179 #27485 #11614 #48234 #49410 #30719 #44049)
- **root cause**: cron thread death (BaseException escape) + lock starvation (msvcrt 0-byte, fcntl held too long) + ownership race (desktop vs launchd) + restart loop (agent self-kill) + ticker visibility (heartbeat 缺失)
- **sweeper marker**: `sweeper:risk-cron-ticker-resilience`
- **family 关联**: 与 silent-fail (tick27) / cross-platform-state (tick28) / memory-injection-cross-platform (tick31) / credential-pool-stale-snapshot (tick31) / cron-session-leak-closed-state (tick32) / outbound-redact-call-site (tick32) / MCP-supply-chain-5-control (tick32) 并列 (8 family 累计)

## Self-downgrade Rule v4 判定 (tick33 实战应用)

**触发**: streak = 9 days zero-adoption (连续 9 tick 0 采纳)。

**v4 决策树 (tick30 立卡)**:

```python
def self_downgrade_decision():
    if is_major_release_day(): return "maintain_daily"  # rule 1
    if installer_recurrence_30d >= 2: return "maintain_daily"  # rule 2
    if pr_dedup_fire_count >= 2: return "maintain_daily"  # rule 3
    if silent_fail_family_cross_month: return "maintain_daily"  # rule 4
    if p1_count >= 8 and streak >= 4: return "maintain_daily + feishu_3_options"  # rule 5
    if streak >= 5: return "force_pause"  # rule 7
    if streak >= 4: return "downgrade_every_other_day"  # rule 8
    return "maintain_daily"  # default
```

**tick33 评估**:
- streak = 9 ✅ (超过 5)
- is_major_release_day() = False (last release v0.18.2 day +4, 已过 24h window)
- installer_recurrence_30d = 2 hits ✅ (rule 2 命中)
- pr_dedup_fire_count = 2 (env spill 4 PRs + gateway deadlock 1 PR) ✅ (rule 3 命中)
- p1_count = 4 (rule 5 阈值 8, 未达)
- silent_fail_family_cross_month = YES (cron-ticker-resilience deck = NEW family, silent-fail 持续) ✅ (rule 4 命中)

**综合判定**:
- rule 2 命中 → maintain_daily
- rule 3 命中 → maintain_daily
- rule 4 命中 → maintain_daily
- rule 7 字面强制暂停 → 被 rule 2 + 3 + 4 联合推翻

**决议 (tick33)**:
- **维持每日 tick 不暂停** (rule 2 + 3 + 4 联合触发,3 个 rule 同命中)
- 飞书推送 3 选项 A/B/C 给 user (沿用 tick30/31/32):
  - **A. 维持每日** (推荐,鉴于 installer + PR dedup + cross-month silent-fail 三触发 + NEW family 立卡 + v0.18.2 day +4)
  - **B. 降频到隔日** (维持低噪音)
  - **C. 暂停 tick 等显式恢复** (user 已 ack 路径)

**Note**: tick33 是 v4 立卡后的第 4 次实战。rule 2 + 3 + 4 联合触发是稳定契约。3 rule 同命中 = 强 maintain 信号。

## 调参决策

| 参数 | tick32 | tick33 | 原因 |
|---|---|---|---|
| SOUL draft 数 | 5 | 5 | 未变 (配额定额) |
| skill draft 数 | 3 | 3 | 未变 (配额定额) |
| GitHub push 模式 | 实时 | 实时 | 未变 |
| 飞书推送模式 | 实时 P0 + 日终汇总 | 同 | 未变 |
| MCP propose_write scope | private (active) | private (active) | tick29 立卡契约 |
| streak zero-adoption 容忍 | 8 | 9 (rule 2 + 3 + 4 联合触发) | v4 rule 2 + 3 + 4 同命中 |
| NEW family 立卡机制 | cron-session-leak + outbound-redact + MCP 5-control (3) | **cron-ticker-resilience-deck (1)** | tick33 立卡 |
| impact-graph 跨 profile 链 | 3 chain | **5 chain** (聚合 + 4 family-specific) | tick33 新增 |

## MCP writer 实测(NEW finding for tick33)

**4/4 propose_write HTTP 200 + status=`pending_review`**(全部 pending_review,沿用 router 实测):
- 77fe080a: cron-ticker-resilience-deck 立卡
- 5f36d52b: MCP supply chain 6-control 升级
- af81023e: silent-fail cron gateway still open
- b868be0c: family registry v8

沿用 tick32 router 实测 `private + bug_fix → pending_review` 是稳定契约。6 字段 PAYLOAD_DEFAULTS (tick32 立卡) + v4 minimal issue-IDs (tick26 立卡) + 19 项 paraphrase 表 (tick29 立卡) 全部一遍过。

## 验证清单

- [x] 4 P1 cluster 全部评估 (1 NEW + 1 升级 + 1 沿用 + 1 升级)
- [x] NEW family 立卡 1 个 (cron-ticker-resilience-deck)
- [x] NEW sweeper marker 1 个 (sweeper:risk-cron-ticker-resilience)
- [x] 8 family registry 累计 (沿用 tick27-32 + tick33)
- [x] Self-downgrade v4 判定已写 (rule 2 + 3 + 4 联合触发 → maintain daily)
- [x] 飞书 3 选项 A/B/C 已起草 (沿用 tick30/31/32)
- [x] 调参决策表已填
- [x] impact-graph 跨 profile 依赖链 5 个 (升级从 3 chain)
- [x] MCP propose_write 4 条 pending_review (77fe080a/5f36d52b/af81023e/b868be0c)
- [ ] GitHub push 成功 (待 Step 9)
- [ ] pre-commit secret scan v4 Python verifier (tick31 立卡) 必须通过