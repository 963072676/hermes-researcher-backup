# 自我审计 2026-07-09 (tick31)

> hermes-researcher-deep-tick-daily tick31
> v0.18.2 (v2026.7.7.2) ship day +1
> streak zero-adoption = **7 days** (tick25-31 全 0 → tick31 进入第 7 天)

## 指标对比

| 指标 | 昨日 (tick30) | tick31 | 趋势 | 状态 |
|---|---|---|---|---|
| 提议数 | 8 (5 SOUL + 3 skill) | 8 (5 SOUL + 3 skill) | = | OK |
| 采纳数 | TBD (pending_review 累计) | TBD | - | 仍待填 |
| 采纳率 | TBD | TBD | - | 仍待填 |
| 误报数 | 0 | 0 | = | OK |
| GH push 失败 | 0 | 0 (待 tick 末确认) | = | OK |
| P0 涌现 | 0 | 0 | = | OK |
| P1 涌现 | 5 | **7** (tick30 5 + tick31 NEW 2: #25205, #40170) | ↑ +40% | spike |
| P2 涌现 | ~35 | ~40 | ↑ +14% | 持续 |
| MCP propose_write | 4 pending_review | (本 tick 4 条 pending_review) | = | OK |
| PR-dedup fire (24h ≥ 3 PR) | 3 | **4** (累加 tick30+tick31) | ↑ +33% | 持续 |
| installer-recurrence 30d | 2 | 2 | = | 持续 |
| memory-injection-cross-platform 30d (NEW) | (未立卡) | **1** (#40170) | NEW | 立卡触发 |
| credential-pool-stale-snapshot 30d | (未立卡) | **1** (#25205) | NEW | 立卡触发 |
| v0.18.x ship | v0.18.1 + v0.18.2 ship day | **v0.18.2 day +1** | patch | 持续 |
| streak zero-adoption | 6 | **7** | ↑ +1 | **触发 v4 rule** |

## tick31 信号密度

- **GitHub**: 27144 open issues total, 3d window active, 5 P1 cluster (tick30) + 2 NEW P1 (#25205, #40170)
- **v0.18.2 (v2026.7.7.2)** 昨日 03:11 UTC ship — patch day +1
- **last push main**: 2026-07-09 14:11Z — 持续 high velocity
- **arXiv / web**: ToolHijacker NDSS 2026 + Claude Code 2.1.196 (2026-07-01) + AttriGuard/AgentVisor/ICON/AgentSentry IPI defense
- **OpenRouter**: 沿用 tick30 snapshot (CN share trend 观察 tick32)
- **MiniMax-M3**: 价格稳定 $0.30/$1.20 USD/M

## tick31 P0/P1 判定

### P0 (open) — 0
延续 tick26+ 的 "P0/P1 zero P0" 状态。

### P1 (open) — 7

**Tick30 持续 (5)**:
1. **#60955** hermes-fallback-bug-report — chief 24h review SLA
2. **#60947** Telegram hygiene no-op — PR dedup fire 2 PRs
3. **#60794** Discord event-loop blocking — PR dedup fire 4 PRs
4. **#47828** provider base_url drift — PR dedup fire 3 PRs (3 天 reassign 检查)
5. **#32617** xAI OAuth fallback — 跨 5 周 P1, fix PR 待开

**Tick31 NEW (2)**:
6. **#25205** (2026-05-13, 60 天 open) `_restore_primary_runtime` credential pool bypass
   - root cause: `run_agent.py:8908` 用 stale snapshot api_key,不 consult credential pool
   - fix PRs: #25206 / #25730 / #53913 (3 PR 抢修)
   - **判定**: chief 6h SLA — primary #53913, 关闭 #25206 / #25730
   - **NEW 立卡 family**: `credential-pool-stale-snapshot`

7. **#40170** (2026-06-07, 60 天 open) Honcho memory leak on customer-facing gateways
   - root cause: `conversation_loop.py:790-796` prefetch_all() + `970-973` cached injection 路径未对 customer-facing gateway 屏蔽
   - 关联: #40967 (closed but wiring missing) + #41003 (follow-up)
   - 影响: WhatsApp / Telegram / Discord / Slack / Signal / Matrix 6 platform 同时泄露
   - **判定**: 跨 6 platform 重大 security, 触发 chief 亲自协调
   - **NEW 立卡 family**: `memory-injection-cross-platform`
   - **sweeper marker**: `sweeper:risk-memory-injection-platform-gateway`

### installer-recurrence 30d (沿用 tick30)

- **#59004** (2026-07-05) — Windows installer web_server.py 合并冲突 ship
- **#60384** (2026-07-07) — Windows hermes_bootstrap.py `hermes update` 后 SyntaxError

## NEW 立卡 family (tick31)

### memory-injection-cross-platform

- **触发**: #40170 (2026-06-07) 跨 ≥ 2 platform label 同时中招
- **root cause**: memory prefetch 路径不区分 CLI/cron session 和 customer-facing gateway session
- **sweeper marker**: `sweeper:risk-memory-injection-platform-gateway`
- **family 关联**: 与 tick28 `cross-platform-state` family 并列 (新立卡,不同 root cause)
- **判定准则**: issue label 集合 ∩ {telegram, discord, slack, whatsapp, signal, matrix} ≥ 2

### credential-pool-stale-snapshot

- **触发**: #25205 (2026-05-13) `_restore_primary_runtime` bypasses credential pool
- **root cause**: restore path 用 stale snapshot, 不 consult pool current best entry
- **sweeper marker**: `sweeper:risk-credential-pool-stale`
- **family 关联**: 与 #15298 / #15434 (exhaustion/cooldown variant) 关联
- **判定准则**: PR 含 `_restore_primary_runtime` 但 diff 不含 `pool.select()`

## Self-downgrade Rule v4 判定 (tick31 实战应用)

**触发**: streak = 7 days zero-adoption (连续 7 tick 0 采纳)。

**v4 决策树 (tick30 立卡)**:

```python
def self_downgrade_decision():
    if is_major_release_day(): return "maintain_daily"  # rule 1
    if installer_recurrence_30d >= 2: return "maintain_daily"  # rule 2
    if pr_dedup_fire_count >= 2: return "maintain_daily"  # rule 3
    if silent_fail_family_cross_month: return "maintain_daily"  # rule 4
    if p1_count >= 8 and streak >= 4: return "maintain_daily + feishu_3_options"  # rule 5
    if streak >= 5: return "force_pause"  # rule 7
    if streak >= 4: return "downgrade_every_other_day"  # rule 8
    return "maintain_daily"  # default
```

**tick31 评估**:
- streak = 7 ✅ (超过 5)
- is_major_release_day() = False (昨日 ship, day +1, 不算 major release day window)
- installer_recurrence_30d = 2 hits ✅ (rule 2 命中)
- pr_dedup_fire_count = 4 (tick30+tick31) ✅ (rule 3 命中)
- p1_count = 7 (rule 5 阈值 8,未达)
- silent_fail_family_cross_month = 未观察到新涌现

**综合判定**:
- rule 2 命中 → maintain_daily
- rule 3 命中 → maintain_daily
- rule 7 字面强制暂停 → 被 rule 2 + rule 3 推翻

**决议 (tick31)**:
- **维持每日 tick 不暂停** (rule 2 + rule 3 命中)
- 飞书推送 3 选项 A/B/C 给 user (沿用 tick30):
  - **A. 维持每日** (推荐, 鉴于 installer + PR dedup 双触发)
  - **B. 降频到隔日** (维持低噪音)
  - **C. 暂停 tick 等显式恢复** (user 已 ack 路径)

**Note**: tick31 是 v4 立卡后的首次实战。rule 2 + rule 3 联合触发是预期路径。

## 调参决策

| 参数 | tick30 | tick31 | 原因 |
|---|---|---|---|
| SOUL draft 数 | 5 | 5 | 未变 (配额定额) |
| skill draft 数 | 3 | 3 | 未变 (配额定额) |
| GitHub push 模式 | 实时 | 实时 | 未变 |
| 飞书推送模式 | 实时 P0 + 日终汇总 | 同 | 未变 |
| MCP propose_write scope | private (active) | private (active) | tick29 立卡契约 |
| streak zero-adoption 容忍 | 6 (实际继续, v2 抗降频) | 7 (rule 2 + 3 联合触发) | v4 rule 2 + 3 命中 |
| NEW family 立卡机制 | (未启用) | **memory-injection-cross-platform + credential-pool-stale-snapshot** | tick31 立卡 |

## 验证清单

- [x] 7 P1 cluster 全部评估 (tick30 5 + tick31 NEW 2)
- [x] NEW family 立卡 2 个 (memory-injection-cross-platform + credential-pool-stale-snapshot)
- [x] NEW sweeper marker 2 个 (sweeper:risk-memory-injection-platform-gateway + sweeper:risk-credential-pool-stale)
- [x] Self-downgrade v4 判定已写 (rule 2 + 3 命中 → maintain daily)
- [x] 飞书 3 选项 A/B/C 已起草 (沿用 tick30)
- [x] 调参决策表已填
- [ ] MCP propose_write 4 条 pending_review (待 Step 7)
- [ ] GitHub push 成功 (待 Step 6)