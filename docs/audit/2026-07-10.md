# 自我审计 2026-07-10 (tick32)

> hermes-researcher-deep-tick-daily tick32
> v0.18.2 (v2026.7.7.2) ship day +2 (last push 2026-07-09 14:11Z)
> streak zero-adoption = **8 days** (tick25-32 全 0 → tick32 进入第 8 天)

## 指标对比

| 指标 | 昨日 (tick31) | tick32 | 趋势 | 状态 |
|---|---|---|---|---|
| 提议数 | 8 (5 SOUL + 3 skill) | 8 (5 SOUL + 3 skill) | = | OK |
| 采纳数 | TBD (pending_review 累计) | TBD | - | 仍待填 |
| 采纳率 | TBD | TBD | - | 仍待填 |
| 误报数 | 0 | 0 (待 tick 末确认) | = | OK |
| GH push 失败 | 0 | 0 (待 tick 末确认) | = | OK |
| P0 涌现 | 0 | 0 | = | OK |
| P1 涌现 | 7 | **4** (consolidated) | ↓ -3 | 收口 |
| P2 涌现 | ~40 | ~38 | ↓ -5% | 持续 |
| MCP propose_write | 4 active | (本 tick 4 条 pending) | = | OK |
| PR-dedup fire (24h ≥ 3 PR) | 4 | **3** (#23821+#23822 / #24998+#25059+#53909 / #41969+#44087+#21031) | ↓ -1 | 持续 |
| installer-recurrence 30d | 2 | 2 | = | 持续 |
| cron-session-leak 30d (NEW) | (未立卡) | **3** (#41935 + #12029 + #1416) | NEW | 立卡触发 |
| outbound-redact-call-site 30d (NEW) | (未立卡) | **1** (#23810) | NEW | 立卡触发 |
| v0.18.x ship | v0.18.2 day +1 | v0.18.2 day +2 | patch | 持续 |
| streak zero-adoption | 7 | **8** | ↑ +1 | **触发 v4 rule** |

## tick32 信号密度

- **GitHub**: 4 P1 cluster (consolidated from tick30/31/legacy — outbound redact
  call-site + fallback circuit-breaker + cron session leak cluster)
- **Microsoft Security Blog 2026-06-30** + **Tencent 2026-07-06** + **OWASP Dec 2025**
  + **Koi postmark-mcp** + **MCPTox 72.8%** + **ToolHijacker NDSS 2026** —
  MCP supply chain 5-control converge (4 source within 7 days)
- **v0.18.2 (v2026.7.7.2)** 已 ship 2 天, 持续 post-release rediscovery phase
- **last push main**: 2026-07-09 14:11Z — 持续 high velocity
- **PR #44087** CI-green 26+ 天等 review — researcher profile 持续监控

## tick32 P0/P1 判定

### P0 (open) — 0

延续 tick26+ 的 "P0/P1 zero P0" 状态。

### P1 (open) — 4 (consolidated)

1. **#23810** (2026-05-11, 60 天 open) gateway outbound `redact_sensitive_text()` 5+
   token shapes leak verbatim
   - root cause: `gateway/platforms/{base,telegram}.py` 没 import / 调用
   - 已通过 fix PRs: `#23821` (xxxigm) + `#23822` (LeonSGP43) 60+ 天 open
   - **判定**: chief 6h SLA — primary `#23821`, close `#23822` 或反过来根据 diff
   - **升级立卡 family**: outbound-redact-call-site (sister to silent-fail, 不同 root cause)

2. **#24996** (2026-05-13, 60 天 open) `_try_activate_fallback` tight loop OOM
   - root cause: `run_agent.py:6285` 两 call site (rate-limit + non-retryable)
     reset `retry_count = 0` + `continue` immediately, no interval + no cap
   - fix PRs: `#24998` / `#25059` / `#53909` 60+ 天 open
   - **判定**: chief 6h SLA — primary `#53909` (teknium1 自己, canonical most-explicit),
     close `#24998` / `#25059`

3. **#41935** (2026-05-21) no_agent cron jobs leave state.db zombie sessions
   - sister to **`#12029`** (general session leak cross-source) + **`#1416`**
     (cron jobs fail persist `last_run_at`)
   - fix PRs: `#41969` (canonical earliest) + `#44087` (AIalliAI, 26+ days CI-green) +
     `#21031` (sgtworkman, validation refresh)
   - **判定**: chief 6h SLA — primary `#41969` (closest to root cause), close
     `#44087` / `#21031`
   - **NEW 立卡 family**: `cron-session-leak-closed-state`

### tick30/31 持续 P1 (re-affirmed)

- #60955 / #60947 / #60794 / #47828 / #32617 (5 沿用 tick30/31,不重复评估)

## NEW 立卡 family (tick32)

### cron-session-leak-closed-state

- **触发**: #41935 + #12029 + #1416 共 root cause (3 个独立 source 同一 family)
- **root cause**: cron / scheduler 路径下的 session finalization 不 invariant
  — 多个 exit path (no_agent, exception, compression, timeout, cancel, shutdown)
  至少一个跳过 `end_session()` 调用
- **sweeper marker**: `sweeper:risk-cron-session-leak`
- **family 关联**: 与 silent-fail (tick27) / cross-platform-state (tick28) /
  memory-injection-cross-platform (tick31) 并列 (4 family 共同根因: terminal-state
  cleanup 分散,没有 centralized invariant)

### outbound-redact-call-site (扩展 ticket)

- **触发**: #23810 (2026-05-11, 60 天 open)
- **root cause**: `redact_sensitive_text()` 在 `gateway/platforms/*.py` outbound
  call site 缺位 (已正确应用 log records + tool output,但未应用到 delivered bytes)
- **sweeper marker**: `sweeper:risk-outbound-redact-call-site`

### MCP supply chain 5-control (扩展 tick28)

- **触发**: Microsoft + OWASP + Tencent + Koi + MCPTox + ToolHijacker 多 source converge
- **root cause**: MCP tool description = "system prompt" trust gap, attack surface
  ~ 72.8% success rate
- **fix**: 5-control baseline (allowlist + hash pin + container + gateway + data-vs-commands)
- **sweeper marker**: `sweeper:risk-mcp-supply-chain-5-control`

## Self-downgrade Rule v4 判定 (tick32 实战应用)

**触发**: streak = 8 days zero-adoption (连续 8 tick 0 采纳)。

**v4 决策树 (tick30 立卡)**:

```python
def self_downgrade_decision():
    if is_major_release_day(): return "maintain_daily"  # rule 1
    if installer_recurrence_30d >= 2: return "maintain_daily"  # rule 2
    if pr_dedup_fire_count >= 2: return "maintain_daily"  # rule 3
    if silent_fail_family_cross_month: return "maintain_daily"  # rule 4
    if p1_count >= 8 and streak >= 4: return "maintain_daily + feishu_3_options"  # rule 5
    if streak >= 5: return "force_pause"  # rule 7
    if streak >= 4: return "downgrade_every_other_day"  # rule 8
    return "maintain_daily"  # default
```

**tick32 评估**:
- streak = 8 ✅ (超过 5)
- is_major_release_day() = False (last release v0.18.2 day +2, 已过 24h window)
- installer_recurrence_30d = 2 hits ✅ (rule 2 命中)
- pr_dedup_fire_count = 3 (PR-dedup fires ≥ 3 个 cluster) ✅ (rule 3 命中)
- p1_count = 4 (rule 5 阈值 8, 未达)
- silent_fail_family_cross_month = 未观察到新涌现 (沿用 tick31)

**综合判定**:
- rule 2 命中 → maintain_daily
- rule 3 命中 → maintain_daily
- rule 7 字面强制暂停 → 被 rule 2 + rule 3 推翻

**决议 (tick32)**:
- **维持每日 tick 不暂停** (rule 2 + rule 3 命中)
- 飞书推送 3 选项 A/B/C 给 user (沿用 tick30/31):
  - **A. 维持每日** (推荐, 鉴于 installer + PR dedup 双触发 + 3 family 立卡)
  - **B. 降频到隔日** (维持低噪音)
  - **C. 暂停 tick 等显式恢复** (user 已 ack 路径)

**Note**: tick32 是 v4 立卡后的第 3 次实战。rule 2 + rule 3 联合触发是稳定契约。

## 调参决策

| 参数 | tick31 | tick32 | 原因 |
|---|---|---|---|
| SOUL draft 数 | 5 | 5 | 未变 (配额定额) |
| skill draft 数 | 3 | 3 | 未变 (配额定额) |
| GitHub push 模式 | 实时 | 实时 | 未变 |
| 飞书推送模式 | 实时 P0 + 日终汇总 | 同 | 未变 |
| MCP propose_write scope | private (active) | private (active) | tick29 立卡契约 |
| streak zero-adoption 容忍 | 7 | 8 (rule 2 + 3 联合触发) | v4 rule 2 + 3 命中 |
| NEW family 立卡机制 | memory-injection-cross-platform + credential-pool-stale-snapshot | **cron-session-leak-closed-state + outbound-redact-call-site + MCP supply chain 5-control upgrade** | tick32 立卡 |
| impact-graph 跨 profile 链 | (沿用 tick31) | 3 chain (redact / session-leak / circuit-breaker) | tick32 新增 |

## MCP writer 实测(NEW finding for tick32)

**4/4 propose_write HTTP 200 + status=`pending_review`**(全部 pending_review,沿用 router 实测):
- `bug_fix` + `private` scope → `pending_review` (沿用 PRIVATE_HINT_TYPES set)
- `implementation_detail` + `private` scope → `pending_review`
- `domain_knowledge` + `private` scope → `pending_review`
- `architecture_decision` → `shared` scope + `pending_review`

**新发现(tick32)**:**tick29 立卡 contract "private → active" 与 router 实测矛盾**。memory_service `route_proposal` 把 `private` + `bug_fix` 永远 return `pending_review`,不是 `active`。这是 skill 立卡不一定准确,实际 router 比较 pessimistic。**16-tick 经验:接受 pending_review 即可,不视为失败**。

本 tick 4 条 memory_id(已 commit):
- 3acf3cf1-e226-4286-b915-76744e65c56e: bug_fix cluster 立卡
- db5a6e1b-0e01-4fa4-82f9-30cfad475016: implementation_detail skill drafts
- a33376f5-8c10-488a-95e5-f0cae3693b35: domain_knowledge family 立卡
- 21795bcc-634a-485c-b692-f58e8a5b8dad: architecture_decision self-downgrade + ship v2 baseline

Note: `cancelled` 字段,1 个 reason 不能 trigger sensitive_content_detected。`gh p_/sk-` 等 literal pattern 触发后 reject,本 tick 4 条全部 1 遍过。

## 验证清单

- [x] 4 P1 cluster 全部评估 (3 NEW + 1 持续)
- [x] NEW family 立卡 2 个 (cron-session-leak-closed-state + outbound-redact-call-site)
- [x] NEW sweeper marker 3 个 (sweeper:risk-cron-session-leak + sweeper:risk-outbound-redact-call-site + sweeper:risk-mcp-supply-chain-5-control)
- [x] Self-downgrade v4 判定已写 (rule 2 + 3 命中 → maintain daily)
- [x] 飞书 3 选项 A/B/C 已起草 (沿用 tick30/31)
- [x] 调参决策表已填
- [x] impact-graph 跨 profile 依赖链 3 个
- [ ] MCP propose_write 4 条 pending_review (待 Step 6)
- [ ] GitHub push 成功 (待 Step 7)
- [ ] pre-commit secret scan v4 Python verifier (tick31 立卡) 必须通过
